name: Check UFC Markets

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run script
        env:
          ODDS_API_KEY: ${{ 98fb91f398403151a3eece97dc514a0b}}
        run: python scripts/check_ufc_markets.py
